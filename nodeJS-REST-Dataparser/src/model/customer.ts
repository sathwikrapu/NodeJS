class Customer {

    constructor(fn, ln, cid) {
        this.firstName = fn;
        this.lastName = ln;
        this.clientId = cid;
    }
    firstName: string
    lastName: string
    clientId: string
}

export default Customer